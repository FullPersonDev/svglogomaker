//Install required packages:
const inquirer = require('inquirer');
const fs = require('fs');

//Import the generatelogo file:
const logoGenerator = require('./utils/generatelogo');

//Write down questions for the prompt:
